import java.math.BigInteger;
import java.util.Formatter;
import java.util.ArrayList;
import java.util.Random;

public class Test
{
   public static void main(String[] args)
   {
      Random random = new Random();

	   //RSAEncryption test = new RSAEncryption("Hello, this is my message and it is very very long it's so long that I'm just going to keep extending it until it is so long that crap gotta go", 1024);
      /*RSAEncryption test = new RSAEncryption("Hello, this is my message", 1024);
	   System.out.println("Cypher: " + test.encrypt());
	   System.out.println("Original: " + test.decrypt());*/
      /*String splitText[]= Common.split("Hello, this is a very long string and I'm going to split it up into different pieces", 3);
      ArrayList<String> numbers = new ArrayList<String>();
      ArrayList<String> text = new ArrayList<String>();
      String total = "";
      for(String current : splitText)
      {
         String temp = Common.makeNumberString(current);
         numbers.add(temp);
      }
      for(String current : numbers)
      {
         System.out.println(current);
         String temp = Common.makeCharString(current);
         text.add(temp);
      }
      System.out.println("Now the characters");
      for(String current : text)
      {
         System.out.println(current);
         total += current;
      }
      System.out.println(total);*/
   }


}
