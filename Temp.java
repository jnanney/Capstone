import java.util.Random;
public class Temp
{
   public static void main(String[] args) throws Exception
   {
      /*byte[] data = new byte[]{64,65,66,67};
      System.out.println("Original is " + java.util.Arrays.toString(data));
      long message = Common.makeBytesLong(data);
      System.out.println("the long is " + message);
      TripleDESEncryption des = new TripleDESEncryption(message);
      long encrypted = des.encrypt();
      System.out.println("Encrypted is " + encrypted);
      byte[] encryptedBytes = Common.makeLongBytes(encrypted);
      System.out.println("Encrypted bytes are " + java.util.Arrays.toString(encryptedBytes));
      long toDecrypt = Common.makeBytesLong(encryptedBytes);
      des.changeOriginal(toDecrypt);
      long decrypted = des.decrypt();
      System.out.println("Decrypted is " + decrypted);
      byte[] result = Common.makeLongBytes(decrypted);
      System.out.println("As bytes it's " + java.util.Arrays.toString(result));*/
   }
}
